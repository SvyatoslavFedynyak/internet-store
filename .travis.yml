language: python
python: 
  - "2.7"
  - "3.6"
services:
  - mysql
env:
  - DJANGO=2.2 DB=mysql
install:
  - pip install -t requirements.txt
before_script:
  - mysql -e 'create database django;' -u django --password='password'